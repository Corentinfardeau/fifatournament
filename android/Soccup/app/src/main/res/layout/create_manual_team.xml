<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="#EAEBFB"
    tools:context=".MainActivity">

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">
        <TextView
            android:text="@string/title_equipes"
            android:id="@+id/titleTeamManual"
            style="@style/title_page" />

        <LinearLayout
            android:id="@+id/layout_content_team_manual"
            android:layout_below="@+id/titleTeamManual"
            style="@style/layout_page">

        </LinearLayout>

        <Button
            style="@style/button_validate"
            android:text="@string/begin" android:id="@+id/btnCreateTeam"
            android:layout_below="@+id/layout_content_team_manual">
        </Button>

    </LinearLayout>
</ScrollView>
